name: Run Daily NOAA GFS Data Ingestion

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-forecast:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install System Dependencies (ecCodes for GRIB2)
        run: |
          sudo apt-get update
          sudo apt-get install -y libeccodes-dev

      - name: Install Python Dependencies
        run: |
          pip install -r requirements.txt

      - name: Run NOAA GFS Ingestion Script
        run: python downloadData.py

      - name: Commit and push updated forecast
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add forecast.geojson
          git commit -m "Update NOAA GFS forecast telemetry [skip ci]" || echo "No changes to commit"
          git push
